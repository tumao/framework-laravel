<?php 

class IndexController extends BaseController
{

	public function index()
	{
		// Session::flush();
		// $groupt = User::find(1)->group;
		// $user = User::find(1);

		// $arr = array('id', 'name', 'email');
		// foreach( $arr as $x)
		// {
		// 	// echo $user->$x;
		// 	Session::push('user.'.$x, $user->$x);
		// }
		// // var_dump( Session::all());
		// $user = Session::get('user');
		// var_dump( $this);
		// var_dump( Appm::environment());
	}

}